package com.example.gui;



/* The interface includes "Login page" and "user ID / password insertion page".
At first, the user can select from 2 options (Login / Continue as Guest).
If "Continue as Guest" selected, it directs to the "List" class.
If user wants to log in using id and password, it directs the user to the list and gives permissions according to the information provided.
 */

public class Authentication {
}
